#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Install pre-commit hook
if [ ! -f ".overcommit.yml" ]; then
    cp .overcommit.yml.example .overcommit.yml
    overcommit
fi
